defmodule Thermos.Thermostat.Setpoint.Hysteresis do
  alias Thermos.Thermostat.Influx.Observation

  def high_band(setpoint) do
  end

  def low_band(setpoint) do
  end

  def save_to_influx(setpoint) do
  end
end
